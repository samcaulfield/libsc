libsc - a C utility library.

Components:
- File: File utility functions.
- Shader: GLSL shader utility functions.

Compilation Instructions:
- Run `make` in the project root directory (same directory as this README).

Dependencies:
- The Shader component requires the OpenGL Extension Wrangler Library (GLEW).

